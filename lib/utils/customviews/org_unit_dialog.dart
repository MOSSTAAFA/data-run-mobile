import 'package:flutter/material.dart';

class OrgUnitDialog extends StatelessWidget {
  const OrgUnitDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
