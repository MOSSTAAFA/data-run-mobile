enum MainScreenNavItem {
  PROGRAMS, //('R.done_task', R.id.menu_home),
  VISUALIZATIONS, //(R.string.done_task, R.id.menu_home),
  QR, //(R.QR_SCANNER, R.id.qr_scan),
  SETTINGS, //(R.SYNC_MANAGER, R.id.sync_manager),
  TROUBLESHOOTING, //(R.main_menu_troubleshooting, R.id.menu_troubleshooting),
  JIRA, //(R.jira_report, R.id.menu_jira),
  ABOUT, //(R.about, R.id.menu_about);

  // const MainScreen(this.title, this.navViewId);

  // final String title;
  // final ViewAction navViewId;
}
